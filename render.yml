services:
  - type: web
    name: flask-task33
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: SECRET_MESSAGE
        value: Task_3_SWE40006
      - key: STUDENT_NAME
        value: Esther
      - key: DATABASE_URL
        fromDatabase:
          name: task33-db
          property: connectionString

databases:
  - name: task33-db
    plan: free